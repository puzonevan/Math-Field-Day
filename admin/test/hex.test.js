const { dfs } = require('./hex-units.js');

const start = {
    row: 0, 
    col: 0,
}

const flag = 0;

describe('.dfs()', () => {
    describe('basic paths', () => {

    });
    describe('blocked paths', () => {

    });
    describe('corner paths', () => {

    });
});